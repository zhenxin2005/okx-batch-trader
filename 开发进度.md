# 开发进度

> 更新日期：2026-09-03
> 配套使用说明见 [README.md](README.md)

## 当前状态

**模拟盘已连通**（2026-09-04）：密钥问题已解决，账户有约 10 万虚拟 USDT。

```
行情实时刷新 ✅ → 筛选 ✅ → 勾选 ✅ → 批量买入 ✅ → 批量卖出 ✅ → 网页配置密钥 ✅ → 模拟盘连通 ✅
                                                                               ↓
                                      模拟盘虚拟交易全流程 ✅（已通过）
```

## 功能清单

| 功能 | 状态 | 说明 |
|------|------|------|
| 网页端 API 配置 | ✅ 完成 | 侧边栏填写密钥，保存写入 .env；实时显示连接状态和错误原因（如 50105） |
| 实时行情表格 | ✅ 完成 | 3/5/10/30 秒自动刷新，只刷新表格区域不卡买卖按钮 |
| 价格区间筛选 | ✅ 完成 | 最低价/最高价，留空不限 |
| 涨跌幅区间筛选 | ✅ 完成 | 24h 涨跌%，留空不限 |
| 交易量/搜索筛选 | ✅ 完成 | 最小交易量 + 关键词 |
| 持仓显示与筛选 | ✅ 完成 | 持仓数量/持仓价值两列 + 「只看已持仓」开关 |
| 表格勾选 | ✅ 完成 | 勾选状态按币种记录，自动刷新/改筛选都不丢 |
| 批量买入 | ✅ 完成 | 每币等额 USDT，余额不足自动拦截 |
| 批量卖出-全部 | ✅ 完成 | 卖出全部持仓，数量自动按精度向下取整防拒单 |
| 批量卖出-按金额 | ✅ 完成 | 每币卖出指定 USDT 金额，OKX 自动折算 |
| 下单结果展示 | ✅ 完成 | 逐币显示成功/失败原因/订单号 |
| 模拟盘/实盘切换 | ✅ 完成 | 网页侧边栏开关（即时生效）或 `.env` 的 `OKX_USE_DEMO`，页面有醒目标识 |

## 待办事项

- [x] ~~创建模拟盘 API 密钥~~（2026-09-04 完成，密钥已通过网页配置面板保存并验证连通）
- [x] ~~领取模拟资金~~（2026-09-04 完成，约 10 万虚拟 USDT）
- [x] **~~模拟盘虚拟交易全流程~~**（2026-09-04 完成：小额买入 → 卖出 → 核对订单 ✅）
- [ ] 若需上实盘：用户自行决定（侧边栏关闭「模拟盘模式」或 `OKX_USE_DEMO=False`）
- [ ] （可选）考虑恢复 SSL 证书验证（当前因走本地代理已关闭，见下方已知问题）

## 已知问题与限制

1. **依赖 OKX 桌面客户端**：默认通过其本地 SOCKS 代理（`127.0.0.1:17001`）联网，客户端没开就连不上行情
2. **SSL 证书验证已关闭**（`okx_client.py` 中 `CERT_NONE`）：走本地代理时的取舍，有中间人风险，仅本机自用可接受
3. **批量下单是逐单顺序执行**：不是原子操作，中途某单失败不影响已成交的订单
4. **历史遗留文件**：`test_websocket.py`、`test_connection.py`、`test_passphrase.py` 是早期调试脚本，与当前代码不完全兼容（如 `test_websocket.py` 引用的 `OKXWebSocketClient` 类已删除），仅作参考
5. 目录下的 `nul` 文件是 Windows 命令误操作产生的空文件，可删除

## 修复记录

| 日期 | 问题 | 修复 |
|------|------|------|
| 2026-09-03 | **买入单位错误（严重）**：下单未传 `tgtCcy`，OKX 会把"100"理解成买 100 个币而不是花 100 USDT | `place_market_buy_order` 增加 `tgtCcy: "quote_ccy"` |
| 2026-09-03 | **模拟盘开关失效**：`.env` 的 `OKX_USE_DEMO` 从未被读取，界面显示和实际下单模式可能不一致 | `OKXTrader.__init__` 读取该环境变量 |
| 2026-09-03 | **新版 Streamlit 报错**：直接给 `st.session_state["market_editor"]` 赋值被禁止 | 改为渲染后读回勾选变动 + 行集合变化时换新表格控件 |
| 2026-09-03 | 过时 API 警告：`use_container_width` 将被移除 | 全部替换为 `width="stretch"` |
| 2026-09-03 | 密钥只能改 `.env` 文件，出错原因不可见 | 新增网页端 API 配置面板：密码式输入框 + 保存写回 .env + 连接状态/错误实时显示，密钥无效时禁用买卖按钮 |
| 2026-09-04 | **Streamlit 1.63 报错**：`st.session_state.confirm_buy` 在控件实例化后被修改 → `StreamlitWidgetAlreadyInstantiatedError` | 按钮不再直接改写控件状态，改为登记 `_pending_widget_reset` 标记，下一轮在控件渲染前统一清空（保存配置、买入、卖出 3 处） |
| 2026-09-04 | **下单 Invalid Sign (50113)**：GET 与 POST 签名规则混用——POST 参数被拼进网址 query，OKX 按不含 query 的路径验签导致不匹配；仅下单失败，查余额正常 | `_request` 区分方法：POST 参数放请求体（无空格紧凑 JSON）、用原始路径签名；GET 保持拼 query 不变 |

## 测试记录

| 日期 | 项目 | 结果 |
|------|------|------|
| 2026-09-03 | 语法检查（py_compile） | ✅ 通过 |
| 2026-09-03 | 无头运行 app.py（Streamlit AppTest） | ✅ 无异常，组件渲染正常 |
| 2026-09-03 | 卖出数量取整逻辑 | ✅ `1.23456789 → 1.234`（步长 0.001） |
| 2026-09-03 | 行情接口连通 | ✅ 274 个 USDT 交易对 |
| 2026-09-03 | 持仓接口连通 | ❌ 返回 `50105 Passphrase incorrect`（现有密钥非模拟盘密钥，待用户更换） |
| 2026-09-03 | 网页端 API 配置面板（AppTest 无头测试） | ✅ 无异常；错误提示正确显示 `50105` 具体原因；密钥无效时买卖按钮禁用 |
| 2026-09-04 | 密钥诊断（`diag_key.py`，模拟盘/实盘双环境测试） | 定位问题：旧密钥为实盘密钥（实盘 ✅ / 模拟盘 50101），更换模拟盘密钥后 **模拟盘 ✅ 账户权益 99,499.91 USDT** |
| 2026-09-04 | Streamlit 1.63 控件状态报错修复（AppTest 无头模拟点单，替换真实下单为假函数） | ✅ 点击买入/卖出不再抛 `StreamlitWidgetAlreadyInstantiatedError` |
| 2026-09-04 | POST 签名逻辑修复（`py_compile` 语法检查） | ✅ 通过；**模拟盘虚拟交易全流程验证通过**（买入+卖出） |
| 2026-09-04 | 模拟盘虚拟交易全流程（小额买入 → 卖出 → 核对订单） | ✅ 通过（交易系统验证完毕） |

## 技术要点备忘

- **网页端 API 配置机制**：密钥存 `st.session_state.api_config`（本次会话生效），点保存时用 `dotenv.set_key()` 写回 `.env`（持久化）；`get_trader()` 用 `@st.cache_resource` **以密钥为参数**缓存，密钥一变自动重建客户端；修改密钥优先级为「显式传参 > `.env` > 默认模拟盘」
- **勾选状态保存机制**：`app.py` 中 `st.session_state.selected` 以币种 ID 为键（如 `BTC-USDT`）记录勾选，与表格行号解耦；表格控件 key 由当前行集合派生，行集合一变就换新控件，防止按行号记录的编辑状态错挂
- **自动刷新机制**：行情表格包在 `@st.fragment(run_every=N)` 里，只重跑表格部分；侧边栏和买卖按钮在 fragment 外，点击立即响应
- **卖出全部的精度处理**：先拉 `/api/v5/public/instruments` 获取每个交易对的数量步长（`lotSz`），持有数量向下取整到步长再下单，避免被 OKX 以精度不符拒单；该数据在进程内只拉一次
- **连接状态检测**：`OKXTrader.check_api()` 返回余额接口原始响应（不吞错误），界面据此显示 ✅/❌ 及具体错误码，密钥无效时禁用买卖按钮
